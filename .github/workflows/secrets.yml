name: workflow_secrets
on:
  workflow_dispatch:
env:
  DB_U: test
  DB_P: password

jobs:
  test-secrets:
    runs-on: ubuntu-22.04
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: checkout env u
        run: echo "u=${{ env.DB_U}}"
      - name: checkout env p
        run: echo "p=${{ env.DB_P}}"